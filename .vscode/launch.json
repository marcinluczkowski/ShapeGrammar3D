{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Rhino",
            "type": "coreclr",
            "request": "attach",
            "processId": "${input:rhinoPid}"
        }
    ],
    "inputs": [
        {
            "id": "rhinoPid",
            "type": "promptString",
            "description": "Rhino PID (Ctrl+V to paste)"
        }
    ]
}
